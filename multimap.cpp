/*
Everything is same as that of map, all functions are same.

EXCEPTIONS AND THINGS TO REMEMBER:
-it can store duplicate keys (any data type)
-it can store duplicate values (any daya type)
-keys are stored in the sorted order
-works in Log n time


we cannot use mp[key] = value; to insert items into the multimap


Q. How are the keys sorted if there can be duplicate keys in multimap.
Ans = They are stored in the order they are inserted
*/

